import{cookies}from"next/headers";import{silverCourse110}from"../../../../lib/silver-course-110-data";import{verifySession}from"../../../../lib/session";import{PasswordGate}from"../../../../components/PasswordGate";import{DivorceCreditCourse}from"../../../../components/DivorceCreditCourse";export const dynamic="force-dynamic";export default async function CoursePage(){const jar=await cookies();const unlocked=await verifySession(jar.get("cp_course_access")?.value,silverCourse110.id);return unlocked?<DivorceCreditCourse course={silverCourse110}/>:<PasswordGate course={silverCourse110}/>}
